<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyun.familyapplication">
    <!-- Camera permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- Internet permission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.Activity.FindActivity"></activity>
        <activity android:name=".view.Activity.ImageActivity" />
        <activity android:name=".view.Activity.MyHomeCheckActivity" />
        <activity android:name=".view.Activity.ProfileActivity" />
        <activity android:name=".view.Activity.ClientActivity" />
        <activity android:name=".view.Activity.TestDB" />
        <activity android:name=".view.Activity.WriteTodoActivity" />
        <activity android:name=".view.Activity.GuideActivity" />
        <activity android:name=".view.Activity.NewsActivity" />
        <activity android:name=".view.Activity.WriteRecordActivity" />
        <activity android:name=".view.Activity.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.Activity.MyHomeActivity"
            android:label="@string/title_activity_my_home" />
        <activity android:name=".view.Activity.MainActivity" />
    </application>

</manifest>